import { NoticeDirective } from './notice.directive';

describe('NoticeDirective', () => {
  it('should create an instance', () => {
    const directive = new NoticeDirective();
    expect(directive).toBeTruthy();
  });
});
